// Ludwig von Schoenfeldt
// 04/24/22
// CS 17
// SID: 1871721
// Quiz1 Question 1


program QuestionOne

#include("stdlib.hhf");

static
input : int8 := 0;
i : int8 := 0;
j : int8 := 0;
T : int8 := 0;

begin QuestionOne; 

stdout.put("Feed Me: ");
stdin.get(input);

mov(T, CH);
cmp(input, CH);
jle Terminate;

//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

mov(input, BL);
mov(BL, i);
mov(0, AL);
dec(i);

UpperWhile:
cmp(i, AL);
je UpperWhileEnd;

//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

mov(0, j);
mov(i, AH);

UpperInner:
cmp(j, AH);
je UpperInnerEnd;

stdout.put(" ");
inc(j);

jmp UpperInner;

UpperInnerEnd:
stdout.put(input);
stdout.newln();


//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

dec(i);
jmp UpperWhile;
UpperWhileEnd:

//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

mov(0,j);
mov(0,i);
mov(input, AL);
WhileLp:

cmp(i, AL);
je WhileLpEnd; //if input == 0 end loop

//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

mov(0, j);
mov(i, AH);

InnerLp:
cmp(j, AH);
je InnerLpEnd;

stdout.put(" ");
inc(j);

jmp InnerLp;

InnerLpEnd:
stdout.put(input);
stdout.newln();

///////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

INC(i);
jmp WhileLp;
WhileLpEnd:

///////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////

Terminate:

end QuestionOne;